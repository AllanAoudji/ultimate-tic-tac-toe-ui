<PlayerBoard />
Each player have one Player component
Each Player have one button

<WinnerFlag />
Display the player who win the game
Have a button used to reset the game

<PlayGameButton />
Button to change screen
Two screen => welcolme and game screen

<OptionButton>
quit game button
save game button

<Game />
Contain two <Player /> the <Board />
deal with all ressources

const onPress = (tileIndex) => () => {
setSelectedTileIndex(tileIndex);
}

Next => style board
